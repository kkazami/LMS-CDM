/**
 * Auto-update module for Lumina LMS Desktop.
 *
 * Currently a placeholder — will be enabled when electron-updater
 * and a release server (e.g., GitHub Releases) are configured.
 *
 * To enable:
 * 1. Install electron-updater: pnpm add electron-updater
 * 2. Configure publish settings in package.json
 * 3. Call setupAutoUpdater() from main.ts
 */

export function setupAutoUpdater() {
  // Placeholder — auto-update will be configured when
  // the app is distributed via GitHub Releases or similar.
  console.log('[AutoUpdater] Auto-update is not configured yet.');
}
